{{- if .Values.workers.mesh.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cube3d.fullname" . }}-worker-mesh
  labels:
    {{- include "cube3d.worker.labels" . | nindent 4 }}
    app.kubernetes.io/stage: mesh
spec:
  {{- if not .Values.workers.mesh.autoscaling.enabled }}
  replicas: {{ .Values.workers.mesh.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "cube3d.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
      app.kubernetes.io/stage: mesh
  template:
    metadata:
      labels:
        {{- include "cube3d.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
        app.kubernetes.io/stage: mesh
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "cube3d.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: worker
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.workers.image.repository }}:{{ .Values.workers.image.tag }}"
          imagePullPolicy: {{ .Values.workers.image.pullPolicy }}
          env:
            - name: TEMPORAL_HOST
              value: {{ include "cube3d.temporalHost" . | quote }}
            - name: TEMPORAL_NAMESPACE
              value: {{ .Values.workers.temporalNamespace | quote }}
            - name: WORKER_MODE
              value: "per-stage"
            - name: WORKER_STAGE
              value: "mesh"
            - name: MESH_TASK_QUEUE
              value: {{ .Values.workers.mesh.taskQueue | quote }}
            - name: MESH_TRITON_URL
              value: {{ .Values.workers.mesh.tritonUrl | quote }}
          resources:
            {{- toYaml .Values.workers.mesh.resources | nindent 12 }}
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import os; exit(0)"
            initialDelaySeconds: 30
            periodSeconds: 30
      {{- with .Values.workers.mesh.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workers.mesh.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workers.mesh.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
